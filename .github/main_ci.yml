name: main_action
on: [push, pull_request]
jobs:
  test:
    uses: ./.github/workflows/test.yml
  build_website:
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'recursive'
      - run: npm install -g npm@latest
      - run: npm install -g @angular/cli
      - run: |
          cd ./ESP_Angular_Frontend
          npm install
          ng build
          mkdir -p ../ESP_Backend/data
          cp -r ./dist/my-app/browser/* ../ESP_Backend/data

  # build_eep_esp32:
  #   needs: [test]
  #   uses: ./.github/workflows/build_eep.yml
  #   with:
  #     core: esp32
  #     board: d1_mini32
  # build_eep_esp8266:
  #   needs: [test]
  #   uses: ./.github/workflows/build_eep.yml
  #   with:
  #     core: esp8266
  #     board: d1_mini

  # build_EEF_packages:
  #   runs-on: ubuntu-latest
  #   needs: [build_eep_esp32, build_eep_esp8266]
  #   env:
  #       TARGET: win64
  #   steps:
  #     - uses: hredan/eef-carefree-action@init
  #       with:
  #         target-os: ${{env.TARGET}}
