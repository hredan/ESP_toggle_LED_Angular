on:
    workflow_call:
      inputs:
        core:
          description: 'ESP core (esp8266 or esp32)'
          default: 'esp32'
          required: true
          type: string
        board:
          description: 'ESP board'
          default: 'd1_mini32'
          type: string
          required: true
jobs:
    build_eep:
        runs-on: ubuntu-latest
        env:
            SKETCH_NAME: ESP_toggle_LED_Backend
        steps:
          - uses: actions/checkout@v4
            with:
              submodules: 'recursive'
          - uses: actions/download-artifact@v4
            with:
              name: data
              path: ./ESP_toggle_LED_Backend/data
          - run: pwd
          - run: ls -la
          - uses: hredan/eep-build-action@main
            with:
              sketch-name: ${{env.SKETCH_NAME}}
              core: ${{ inputs.core }}
              board: ${{ inputs.board}}
              sketch-path: './ESP_toggle_LED_Backend'
          - run: ls -la